@model TestService.Models.ProductsCategory

@{
    ViewBag.Title = "Delete";
}

<h2 class="text-center" style="color:#020240">Delete Your Product Category</h2>
<div class="container mb-3">
    <h3 style="margin-left:370px;color:#ff0000">Are you sure you want to delete this?</h3>
    <div>
        <dl class="dl-horizontal ">
            <dt>
                @Html.DisplayNameFor(model => model.categoryName)
            </dt>

            <dd class="form-control">
                @Html.DisplayFor(model => model.categoryName)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color  col-md-offset-2">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index",new { },new {@class="btn btn-info btn-sm"})
            </div>
        }
    </div>
</div>
